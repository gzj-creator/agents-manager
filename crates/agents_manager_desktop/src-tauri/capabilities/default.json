{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main desktop window to use the default Tauri core APIs, including event listeners required by drag-and-drop import.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
